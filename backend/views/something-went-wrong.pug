doctype html
html(lang="en")
    head
        title 404 - Page Not Found
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        script(src="https://cdn.tailwindcss.com")

    body(class="bg-gray-100 font-sans text-gray-800")
        include topbar
        + topbar()

        .container(class="flex flex-col items-center justify-center min-h-screen px-8 pt-20")
            h1(class='text-2xl font-semibold text-gray-700') Something Went Wrong!
            p(class='text-lg text-gray-600') Sorry, we encountered an unexpected error.
            img(class='w-40 h-40 object-cover mt-4' src="https://i.imgur.com/qIufhof.png" alt="Lost")